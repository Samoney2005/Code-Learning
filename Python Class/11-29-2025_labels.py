from tkinter import *
root = Tk()
#changes the size of window
root.geometry("100x100")
#add a label to the window
w = Label(root, text = "GUI")
# connect the label to the window object "root in our case"
w.pack()
# keeps window open
window.mainloop()